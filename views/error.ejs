<!-- Hata Sayfası -->

<div class="container py-5 text-center">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <% if (error.status === 404) { %>
        <i class="fas fa-search display-1 text-primary mb-4"></i>
        <h1 class="display-4 mb-3">Sayfa Bulunamadı</h1>
        <p class="lead mb-5">
          Aradığınız sayfa bulunamadı. Sayfa kaldırılmış, adı değiştirilmiş veya geçici olarak kullanılamıyor olabilir.
        </p>
      <% } else { %>
        <i class="fas fa-exclamation-triangle display-1 text-danger mb-4"></i>
        <h1 class="display-4 mb-3">
          <% if (error.status === 403) { %>
            Erişim Reddedildi
          <% } else if (error.status === 500) { %>
            Sunucu Hatası
          <% } else { %>
            Hata Oluştu
          <% } %>
        </h1>
        <p class="lead mb-2">Maalesef bir hata oluştu.</p>
        <p class="mb-5">
          <%= error.message || 'Beklenmeyen bir hata oluştu. Lütfen daha sonra tekrar deneyin.' %>
        </p>
      <% } %>
      
      <div class="d-flex justify-content-center gap-3">
        <a href="/" class="btn btn-primary">
          <i class="fas fa-home me-2"></i> Ana Sayfaya Dön
        </a>
        <button class="btn btn-outline-secondary" onclick="window.history.back()">
          <i class="fas fa-arrow-left me-2"></i> Geri Dön
        </button>
      </div>
    </div>
  </div>
</div>